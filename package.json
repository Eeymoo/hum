{
  "name": "hum",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/cli",
    "packages/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "next-intl": "^4.12.0"
  }
}
