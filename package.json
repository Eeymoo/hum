{
  "name": "hum",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "packages/cli"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "next-intl": "^4.12.0"
  }
}
